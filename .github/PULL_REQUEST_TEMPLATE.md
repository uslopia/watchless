## What

<!-- One or two sentences. Link the issue: Fixes #123 -->

## Why

<!-- The problem this solves. Skip if the issue already says it. -->

## How it was checked

<!-- Commands you ran, and what you clicked through in the browser. -->

- [ ] `npm run check` passes
- [ ] `npm test` passes
- [ ] Loaded `dist/` in Chrome and exercised the change
- [ ] `_locales/{en,fr}` updated if a UI string, format, preset or check changed
